import React from 'react';

interface IProps {}
interface IState {}

const AppStatus = (props: IProps, state: IState) => {
  return (
    <h1>~~~ AppStatus ~~~</h1>
  );
};

export default AppStatus;
